'use strict';
require("reflect-metadata");
describe('MlclDatabase', function () {
    it('should be possible to load the database config', () => {
    });
    it('should be possible to init all configured Database interfaces', () => {
    });
    it('should be possible to access the database connections', () => {
    });
    it('should be possible to store data in persistence layer', () => {
    });
    it('should be possible to read data from the persistence layer', () => {
    });
    it('should be possible to populate data', () => {
    });
    it('should be possible to store and populate data in the population layer', () => {
    });
    it('should be possible to read data from the population layer', () => {
    });
});
